# HANDOFF — maimai でらっくす RATING 計算ツール（PoC 検証中）

> 引き継ぎ用メモ。ブランチ: `feat/maimai-dx-rating-poc`（origin/main 起点、**PR 未作成**）
> 最新コミット: `3f1bc5b5`（全コミット push 済み）
> 作成: 2026-08-30 / 更新: 2026-08-31

## 1. プロジェクトの現状

maimai でらっくす RATING 計算ツールの **Python3 PoC を実装・実データ検証中**。
技術スタックは本実装時未定（ADR-0002）。PoC は「動く仕様」であり、本実装は別スタックで再実装する前提。

- 仕様の正: `docs/domain.md`（公式ソース引用 + 調査結果）
- 開発手順・表記規則: `docs/conventions.md`（出典は公式のみ・「§」参照禁止・PR は勝手に作らない）
- 技術決定: `docs/adr/`（ADR-0001: 定数 DB は JSON 先行 → ビューア時に RDB 移行 / ADR-0002: 技術スタック未決定）
- ルート AGENTS.md に PR マージ方針（基本 squash）追記済み

**2026-08-31 時点の進捗**: 入力方式 3 種（コピペテキスト / ページ保存 HTML / ブックマークレット JSON）を実装。
12+ / 13 / 13+ の実データ 3 ページで統合計算まで完了（衝突 0・未解決 0）。現在は**較正（実 RATING との突合）**段階。
ユーザーの実 RATING 値は**リポジトリに記載しない**（プライバシー規則。較正の進捗は数値なしで記録する）。

## 2. コード構成（poc/ + tools/）

| ファイル | 内容 |
| --- | --- |
| `rating_core.py` | 純関数: 単曲レート（整数演算・寸止め係数込み）/ 枠選定（新曲15+ベスト35・Re:M 例外）/ version 帯判定（フロア） |
| `net_parser.py` | 入力パーサ: コピペテキスト（LEVEL 一覧 + バージョン別ページ）/ ページ保存 HTML / ブックマークレット JSON。統計ブロックスキップ、衝突検出、未プレイ分離 |
| `run.py` | マスタ + 入力 + 定数 → RATING + CSV 2 本。入力の自動判定（HTML / JSON）。`--difficulty` / `--current-version` / `--master` / `--constants` オプション |
| `constants.json` | 検証用サンプル 12 譜面（手入力の**仮値**。実データ評価では使わない） |
| `test_rating.py` | unittest **54 件**（`python3 -m unittest poc/test_rating.py` で実行） |
| `example_paste.txt` | ダミーコピペ（統計ブロックはダミーラベル） |
| `example_bookmarklet.json` | ダミーのブックマークレット出力（ST/DX 同居・未プレイを含む） |
| `README.md` | 使い方・仮定 10 項目・既知の制限（2026-08-31 に 3 方式対応で更新済み） |
| `tools/bookmarklet.html` | スコア抽出ブックマークレット（DOM → JSON ダウンロード。設置方法・出力形式の説明込み。取得ファイル名に日時入り） |

実行例:
```bash
cd projects/_labs/maimai_dx_rating_calc
python3 poc/run.py <コピペ or HTML or JSON> <出力先> --constants data/maimai_constants.json
```

## 3. データファイル（data/ に集約・.gitignore 除外）

| ファイル | 内容 | 扱い |
| --- | --- | --- |
| `data/maimai_constants.json` | **譜面定数 DB 6364 譜面 / 1480 曲**（非公式データ収集、CiRCLE PLUS 時点、0.1 刻み・全難易度・表示Lv はマスタと 6364/6364 一致確認済み） | 内部利用のみ・**git 除外**。出典サイト名は文書に記載しない。DB 設計時に再整理 |
| `data/maimai_songs.json` | 公式楽曲マスタ 1571 曲（run.py が自動キャッシュ） | 公式データ・git 除外 |
| `data/pastes/level_list.txt` | **ユーザーの実スコア（LEVEL 一覧ページのコピペ）** | **個人情報。git 除外（ユーザー明示）** |
| `data/pastes/version_master.txt` | **ユーザーの実スコア（バージョン別・MASTER ページ）** | 同上 |
| `/tmp/maimainet/`（リポジトリ外） | **ユーザーの実スコア（ブックマークレット JSON: level18/19/20 = 12+/13/13+）** | 同上（git 対象外の /tmp にあり） |

## 4. 実データ検証の結果（2026-08-30〜31）

- **単一 LEVEL の一覧のみ**（コピペ）: RATING 10001（新曲枠 718 / ベスト枠 9283 / AP 0）。プレイ済み全曲のパース・定数照合に成功
- **CiRCLE PLUS / MASTER ページ + `--difficulty MASTER`**（コピペ）: RATING 4433（新曲枠 15 満杯 / ベスト枠 0 / AP 14）→ このページの曲は全員新曲枠候補で正しい
- **12+ / 13 / 13+ の 3 ページ統合**（ブックマークレット JSON・2026-08-31）: 1388 エントリ（プレイ済み 1207 / 未プレイ 181）をパース。**警告 0・衝突 0・未解決 0**。統合 RATING は新曲枠 15 満杯 + ベスト枠 35 満杯（詳細数値は data 非公開扱い。出力 CSV は /tmp にあり）
  - この 3 ページの ST/DX 同居は全曲確定できた（当初の懸念だった「13/13+ の MASTER 同居 22 譜面」がコピペではなく HTML / JSON 方式で解決）
  - **較正開始**: 上記の計算値と NET 表示の実 RATING に**ズレあり**（原因候補: 新曲枠判定＝マスタ version は ST 譜面セット基準、AP 近似判定、Rank 係数表、切捨位置。domain.md『未確定ポイント』の 1 を更新）。実値は Git に記載しない
- **新発見（重要）**: 魔理沙は大変なものを盗んでいきました DX MASTER 13+ は、公式マスタの version=12002（GreeN 帯）だが **NET バージョン別ページでは CiRCLE PLUS 掲載** → マスタ version は「ST 譜面セット追加バージョン」であり **DX 譜面の後からの追加は拾えない実例**（domain.md 仮定 4 を反証）。**NET バージョン別ページが譜面単位の追加バージョンの正**。コードはページ version 優先で対応済み（`to_scored_chart`）
- **タイトルなし曲（2026-08-31 発見）**: 曲名が全角スペース 1 文字の曲が 1 曲存在（x0o0x_ 氏の楽曲、UNiVERSE PLUS 追加）。NET / 定数 DB / マスタとも「　」表記。ブックマークレットと HTML パーサの trim で消える問題を修正済み（曲名として保持）。コピペではパース不可（known limitation）
- 衝突 3 曲（難易度不明のため除外）: 火炎地獄 / ジングルベル / System "Z"（いずれも ST/DX が同一 LEVEL の MASTER 譜面。ブックマークレット JSON では確定できるため実質解決。ジングルベルは難易度別ページで解消可）

## 5. NET の URL 仕様（実測）

- LEVEL 一覧: `https://maimaidx.jp/maimai-mobile/record/musicLevel/search/?level=19`（level = 内部 Lv インデックス。Lv13=19, Lv15=23。式: N + max(0, N−7)、+ はさらに +1）
- **バージョン別**: `https://maimaidx.jp/maimai-mobile/record/musicVersion/search/?version=26&diff=3`
  - `version` = 0 始まり連番（0=maimai 初代 … **26=CiRCLE PLUS**。27 バージョン分）
  - `diff` = 難易度（0=BASIC, 1=ADVANCED, 2=EXPERT, 3=MASTER 実測。4=Re:MASTER と推測）
  - コピペ形式は LEVEL 一覧と同一（ヘッダ行がバージョン名になるだけ）。**難易度はテキストに落ちない → CLI で `--difficulty` 指定**

## 6. ユーザー待ち・未着手

1. **較正**: 実 RATING 値は入手済み（数値は Git 非公開）。12+ / 13 / 13+ の計算値とのズレの原因特定が次の主作業。正解データになり得るのは NET サブスクの「RATING 対象曲」表示（要検証・未確認）
2. **データの GitHub 不保持方針（決定）+ 取り込みツール（構想）**: マスタ・定数 DB は GitHub に持たない（`data/` を .gitignore 除外、既に移設済み）。再現性のため**取り込みコマンド（公式 URL 取得・定数収集の自動化）を検討中**（構想段階）。DB 設計（ADR-0001 の RDB 移行）時に整理する
3. **NET サブスクの「RATING 対象曲」表示の実測確認**: 枠判定の正解データとして利用可否（domain.md『参考: NET サブスクリプションの「RATING 対象曲」表示（要検証）』参照）
4. **バージョン別ページの diff=4（Re:MASTER と推測）の確認**: ユーザーの 14 以降はベスト記録が無いため、現状のデータでは確認手段が限られる（任意）
5. **曲名重複（Link 2 曲・無題曲）の照合キー強化（id 導入）**: 本実装時
6. **統計ブロック 21 行の並びと意味の確定**: 実データで数字のみ検証済み（ラベルは未確認）
7. **PR 作成・push 判断**: ユーザー指示待ち（現状: ブランチへの commit + push のみ）

## 7. 次にやること（引き継ぎ先向け）

1. **較正の原因特定**: 実 RATING と 12+〜13+ 計算値のズレ（数値・詳細は非公開）を切り分ける。
   (a) 新曲枠判定: ツールの新曲枠 15 曲が「マスタ version 基準（ST 譜面セット）」で選ばれており、NET の譜面単位基準と異なる曲が混在していないか → バージョン別ページ（CiRCLE PLUS / CiRCLE）をブックマークレットで取得して突き合わせると確定できる
   (b) AP 近似判定: 達成率 100.00% 以上を全部 AP とみなしている（domain.md『AP ボーナス』の近似）。誤差の可能性
   (c) Rank 係数表・切捨位置: コミュニティ調査値のため再確認
2. **取り込みツールの構想を具体化**（マスタ取得コマンド → 定数収集の自動化 → DB 設計時に整理）
3. **ドキュメント整備**: domain.md・README（2026-08-31 に更新済み）。新しい実測が出たら随時追記

## 8. 環境メモ

- Python 3.14.4（stdlib のみ使用、依存なし）
- Herdr: ペイン wE:p1Z（親）/ wE:p31（poc 実装済み・アイドル）/ wE:p32（定数調査済み・アイドル）
- ユーザーの個人スコアデータは絶対に Git に入れない（明示指示済み）

## 9. 後日検証チェックリスト

- [x] 12+ / 13 / 13+ の入力での統合 RATING 計算（ブックマークレット JSON、2026-08-31）→ 衝突 0・未解決 0
- [~] **NET 表示の実 RATING との較正**: ズレを確認（原因候補: 新曲枠判定・AP 近似・係数表・切捨位置）。枠内 50 譜面の正解データ（サブスク表示など）が要る
- [ ] NET バージョン別ページの `diff=4` が Re:MASTER かどうかの確認（現状 0=BASIC / 3=MASTER のみ実測）
- [ ] NET サブスク（月額 330 円）の「RATING 対象曲」表示の実測確認 → 枠判定の正解データとして利用可否（domain.md『参考: NET サブスクリプションの「RATING 対象曲」表示（要検証）』参照）
- [ ] 統計ブロック 21 行の並びと意味を実データで確定（example_paste.txt はダミーラベルのまま。実コピペは数字のみで検証済み）
- [ ] 魔理沙ケース（DX 後追加）が現行ウィンドウ内に他にも無いか、バージョン別ページとマスタ version の突合で調査
- [~] ブックマークレット JSON の page.version / page.diff（バージョン別ページ取得時）を record へ反映する対応（現状は LEVEL ページ専用の扱い。魔理沙ケースの原因特定に有効）
- [ ] 定数 DB の取り込みツール構想の具体化（マスタ取得コマンド・定数収集の自動化。DB 設計時に整理）
- [x] 衝突 3 曲（火炎地獄 / ジングルベル / System "Z"）: ブックマークレット JSON で ST/DX が確定するため実質解決（コピペ時のみ衝突のまま）
- [ ] 曲名重複（Link 2 曲・無題曲）の照合キーへの id 導入（本実装時）
  - タイトルなし曲（全角スペース 1 文字）は定数 DB・マスタ・NET が同表記のため照合可能（2026-08-31 確認）