{
  "name": "file-server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "lint": "biome lint --write",
    "format": "biome format --write",
    "test": "bun test"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.2",
    "hono": "^4.8.5",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.2",
    "@types/bun": "latest"
  }
}
