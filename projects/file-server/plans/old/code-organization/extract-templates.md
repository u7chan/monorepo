# Extract JSX/HTML Templates

**Priority:** Low  
**Category:** Code Organization

## Task

Extract JSX/HTML from route handlers

## Requirements

- Move templates to separate files (e.g., `index.tsx`)
- Keep route handlers logic-only

## Acceptance Criteria

- [ ] Templates moved to separate files
- [ ] Route handlers contain only logic
- [ ] No JSX in route handler files
- [ ] Clean separation of concerns
